# Introducción
Este es mi primer programa en Node. 
```
Options:
      --help     Show help                                           [boolean]
      --version  Show version number                                 [boolean]
  -b, --base     Es la base de la tabla de multiplicar, intentalo
                                                           [number] [required]
  -l, --listar   Para listar o no la tabla con múltiplos
                                                    [boolean] [default: false]
  -h, --hasta    El número n que multiplica a la base [number] [default: "10"]
```
